{
  "name": "bunxy",
  "module": "index.ts",
  "type": "module",
  "license": "ISC",
  "scripts": {
    "start": "bun run example/index.ts",
    "dev": "bun --watch run example/index.ts",
    "dev:test": "bun --watch run testServer/index.ts",
    "lint": "tsc && prettier --check .",
    "prettier": "prettier --write .",
    "prepare": "husky install"
  },
  "devDependencies": {
    "bun-types": "^1.0.4",
    "husky": "^8.0.3",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "peerDependencies": {
    "typescript": "^5.2.2"
  },
  "prettier": {
    "singleQuote": true
  }
}
